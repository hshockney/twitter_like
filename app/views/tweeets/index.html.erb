<br>
<br>





<div class="album py-5 bg-light">
        <div class="container">

          <div class="row">

<% if user_signed_in? %>
         <%= render 'profile' %>
<% else %>

<%= render 'trends' %>

<% end %>

<%= render 'feed' %>


<%= render 'who-to-follow' %>

 </div>
        </div>
      </div>